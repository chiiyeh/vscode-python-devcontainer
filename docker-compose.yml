version: "3.7"
services:
  dev:
    build:
      context: .
      args:
        user: "$USER"
    container_name: dev_container
    network_mode: "host"
    volumes:
    - type: bind
      source: ./
      target: /home/${USER}/dev
    command: "tail -f /dev/null"